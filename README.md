# Kanban frontend

Simple kanban board with drag and drop functionality

## Running

When running locally the backend is expected to be at http://localhost:8000.

To start cd to the root of the repo and run:

```console
npm run build && npm run start
```

The app will be running at http://localhost:3000